{
  "name": "subtrack-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:render": "vite build --config vite.test.config.js --mode render --logLevel warn && node .ssr-out/render.js && vite build --config vite.test.config.js --mode client --logLevel warn && node --import ./test/jsdom-env.mjs .client-out/client.js"
  },
  "dependencies": {
    "vue": "^3.5.13",
    "vue-router": "^4.5.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "jsdom": "^30.0.1",
    "vite": "^6.0.7"
  }
}
